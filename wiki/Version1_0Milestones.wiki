#summary SlideShow Android Component Version 1.0 Milestones
#labels Phase-Requirements

= Introduction =

This document describes plans for the 1.0 release of the SlideShow Android component.


= Details =

  # Add junit test coverage for all component classes (not necessarily example/demo).
  # Test on G1 device.
  # Fix all bugs exposed in testing.
  # Modify pom for jar publication of com.freebeachler.android.component.slideshow as a "download".
  # Modify pom for apk publication of slideshow example as a "download".
  # Add/Extend interface(s) to listen for touch/key events on the slide itself.